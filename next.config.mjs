/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "cdn-icons-png.flaticon.com", "avatars.githubusercontent.com"],
  },
};

export default nextConfig;
